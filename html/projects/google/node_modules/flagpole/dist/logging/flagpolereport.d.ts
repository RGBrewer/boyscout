import { FlagpoleExecutionOptions } from "../flagpoleexecutionoptions";
import { iConsoleLine, iSuite } from "../interfaces";
export declare class FlagpoleReport {
    readonly suite: iSuite;
    readonly opts: FlagpoleExecutionOptions;
    constructor(suite: iSuite, opts: FlagpoleExecutionOptions);
    toConsole(): Promise<iConsoleLine[]>;
    toJson(): Promise<any>;
    toHTML(): Promise<string>;
    toDelimited(format: string): Promise<string[]>;
    print(): Promise<any>;
    toString(): Promise<string>;
}
