export interface iKeyChain {
    get(service: string, account: string): Promise<iCredentials>;
    set(service: string, account: string, password: string): Promise<iCredentials>;
    remove(service: string, account: string): Promise<any>;
}
export interface iCredentials {
    account: string;
    password: string;
}
export declare class ClorthoService {
    protected serviceName: string;
    constructor(serviceName: string);
    get(account: string): Promise<iCredentials>;
    set(account: string, password: string): Promise<iCredentials>;
    remove(account: string): Promise<any>;
}
export declare const clortho: (serviceName: string) => ClorthoService;
